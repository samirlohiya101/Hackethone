import React from 'react';
import MapboxAutoComplete from '../components/MapboxAutoComplete';

const Home = () => {
    return (
        <div>
            <h1>Mapbox Autocomplete Example</h1>
            <MapboxAutoComplete  />
        </div>
    );
};

export default Home;
